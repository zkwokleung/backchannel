<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window background is what shows for the frame between the splash handing off and Compose
         drawing. It has to match the Compose background per theme, or the app flashes the wrong
         colour on launch — see values-night for the dark variant. -->
    <style name="Theme.Backchannel" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.Backchannel.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_brand_mark</item>
        <item name="postSplashScreenTheme">@style/Theme.Backchannel</item>
    </style>
</resources>
